# Pinned for matrix-test reproducibility. Bump deliberately when
# upgrading; CI runs against the same pin so a driver-side wire-format
# change can't silently regress what we believe we're testing.
neo4j==5.28.1
pytest==8.3.4
